#pragma once
#include <vector>

using namespace std;

struct DProperties
{
	vector<int>* domainindexs; //Will be selected based on type of object holding it
	vector<int> locationindex;
};

